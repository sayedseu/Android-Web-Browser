<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <WebView
        android:id="@+id/webViewID"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </WebView>

    <com.daimajia.numberprogressbar.NumberProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        app:progress_unreached_color="@color/ProgressUnreachedColor"
        app:progress_reached_color="@color/ProgressReachedColor"

        app:progress_unreached_bar_height="2.5dp"
        app:progress_reached_bar_height="5dp"

        app:progress_text_size="10sp"
        app:progress_text_color="@color/ProgressTextColor"
        app:progress_text_offset="1dp"
        app:progress_text_visibility="visible"

        app:progress_max="100"
        android:visibility="gone"
        />

</FrameLayout>